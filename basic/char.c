#include <stdio.h>

int main() {
  char ch;

  // int i;
  //
  // i = 'a';
  // ch = 65;
  //
  // ch = ch + 1;
  // ch++;
  //
  // if ('a' <= ch && ch <= 'z')
  //   ch = ch - 'a' + 'A';
  // printf("%c\n", ch);
  //
  // for (ch = 'A'; ch <= 'Z'; ch++)
  //   printf("%c\n", ch);

  ch = 'a' * 'b' / 'c'; // `
  printf("%c\n", ch);

  return 0;
}
